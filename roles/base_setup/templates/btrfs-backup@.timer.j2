[Unit]
Description=Daily off-box btrfs backup to target %i

[Timer]
OnCalendar=01:00:00
Persistent=true
# Targets run independently, so one slow disk does not hold up another.
RandomizedDelaySec=300

[Install]
WantedBy=timers.target

# Example environment overrides for ansible-base
# Source this file or pass vars directly to ansible-playbook.

# Deployment host
ANSIBLE_HOST=host.containers.internal
ANSIBLE_PORT=2222
ANSIBLE_USER=core

# These correspond to role defaults — override via --extra-vars or group_vars.
# See roles/base_setup/defaults/main.yml for the full list.
